import React, { useEffect, useState } from 'react';
// import ChatListBox from './ChatListBox';

/**
 * fetch lists of chats from a username
 */

// export default function ChatListBar({ user }: { user: string }) {
//   const [chatList, setChatList] = useState<string[]>([]);

//   async function getCharList(name: string) {
//     //TODO: fetch from a database to get a list of chats from the user
//     //Sort by most recent message
//     //Set the chatlist array state
//   }
  
//   useEffect(() => {
//     getCharList(user);
//   }, []);

//   return (
//     <div>
//       {chatList.map((ChatID) => {
//         return <ChatListBox name={ChatID} />;
//       })}
//     </div>
//   );
// }
